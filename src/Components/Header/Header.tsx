import { IonHeader, IonTitle, IonToolbar } from "@ionic/react";

export default function Header() {
  return (
    <IonHeader>
      <IonToolbar>
        <IonTitle>World's Countries</IonTitle>
      </IonToolbar>
    </IonHeader>
  );
}
